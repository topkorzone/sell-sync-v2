server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: marketplace-hub
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0}
    username: ${DB_USERNAME:postgres.nnwcuhamxjqgmyzptbyb}
    password: ${DB_PASSWORD:Qortlqdjr1!}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-init-sql: "SET app.current_tenant_id = '00000000-0000-0000-0000-000000000000'"

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 100
          batch_versioned_data: true
        order_inserts: true
        order_updates: true
        default_batch_fetch_size: 50

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    url: ${FLYWAY_DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?prepareThreshold=0}
    user: ${DB_USERNAME:postgres.nnwcuhamxjqgmyzptbyb}
    password: ${DB_PASSWORD:Qortlqdjr1!}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 3000

  task:
    scheduling:
      pool:
        size: 5

  lifecycle:
    timeout-per-shutdown-phase: 30s

# CJ API
cj:
  api:
    base-url: ${CJ_API_BASE_URL:https://dxapi-dev.cjlogistics.com:5054}

# Encryption & Supabase
mhub:
  encryption:
    key: ${ENCRYPTION_KEY:bG9jYWwtZGV2LWVuY3J5cHRpb24ta2V5LTMyY2hhciE=}
  supabase:
    url: ${SUPABASE_URL:https://nnwcuhamxjqgmyzptbyb.supabase.co}
    jwt-secret: ${SUPABASE_JWT_SECRET:4kKRsBdu//RkAbzadWF5ULX+qWTPX0UaXbOCZE7qivjtQ10CMlR9Vm+Y+aWRZCu063M4exLP8W9YED5BT4Zp3Q==}
    service-role-key: ${SUPABASE_SERVICE_ROLE_KEY:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5ud2N1aGFteGpxZ215enB0YnliIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2OTk1ODMxMSwiZXhwIjoyMDg1NTM0MzExfQ.7I42NWYIBHzKSz0tnny7l0o8qlNM_3Q-0iRtx-o1GpY}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# SQS
# cloud:
#   aws:
#     region:
#       static: ${AWS_REGION:ap-northeast-2}
#     sqs:
#       endpoint: ${SQS_ENDPOINT:}
#     credentials:
#       access-key: ${AWS_ACCESS_KEY_ID:test}
#       secret-key: ${AWS_SECRET_ACCESS_KEY:test}

# Scheduler
shedlock:
  defaults:
    lock-at-most-for: PT30M

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized

# Logging
logging:
  level:
    root: INFO
    com.mhub: DEBUG
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{tenantId}] %logger{36} - %msg%n"

# Springdoc
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
